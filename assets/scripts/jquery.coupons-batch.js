// Generated by CoffeeScript 1.5.0
(function() {
  var $;

  $ = jQuery;

  $(function() {
    return $('.postbox#lowtone_woocommerce_coupons_batch').each(function() {
      var $fieldset, $postbox, disable, inject, injected;
      $postbox = $(this);
      $fieldset = $postbox.find('#lowtone_woocommerce_coupons_batch_options');
      disable = function(disabled) {
        if (disabled == null) {
          disabled = true;
        }
        $fieldset.toggleClass('disabled', disabled);
        return $fieldset.find('input').attr('disabled', disabled);
      };
      injected = false;
      inject = function() {
        var $title, val;
        if (injected) {
          return;
        }
        $title = $('#title');
        val = $title.val();
        if ('' !== val) {
          val += '-';
        }
        val += '%s';
        $title.val(val);
        $('label[for="title"]').addClass('screen-reader-text');
        return injected = true;
      };
      $postbox.find('input[type="checkbox"]').first().change(function() {
        var checked;
        checked = $(this).is(':checked');
        disable(!checked);
        if (checked) {
          return inject();
        }
      });
      return disable(true);
    });
  });

}).call(this);
